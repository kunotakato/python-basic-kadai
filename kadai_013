# 与えられた引数に＄消費税（tax）を加算し、その値を出力する関数を定義する
def calculate_total(price,tax):

  #与えられた引数priceに消費税（tax:10%）を加算し、変数totalに代入
  total = price + (price * tax)
  return total

 # 関数を呼び出し。引数として購入金額を出す。
total = calculate_total(1000,0.1)

print(total)
